diff --git a/index.html b/index.html
index ca0882b..af1652c 100644
--- a/index.html
+++ b/index.html
@@ -144,6 +144,33 @@ body{font-family:'Rubik',sans-serif;background:var(--bg);color:var(--text);
   display:flex;align-items:center;justify-content:center;flex-shrink:0;
   transition:transform .1s,opacity .1s;}
 .add-btn:active{transform:scale(.85);opacity:.85}
+/* ── TEXT IMPORT BUTTON ── */
+.import-btn{background:var(--card);border:1.5px solid var(--border);
+  border-radius:11px;width:40px;height:40px;font-size:16px;cursor:pointer;
+  display:flex;align-items:center;justify-content:center;flex-shrink:0;
+  transition:opacity .1s;}
+.import-btn:active{opacity:.7}
+/* ── TEXT IMPORT MODAL ── */
+.import-overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,.45);
+  z-index:1100;align-items:flex-end;justify-content:center;}
+.import-modal{background:var(--surface);border-radius:20px 20px 0 0;
+  padding:16px 18px calc(24px + var(--safe-b));width:100%;max-width:520px;
+  max-height:70vh;display:flex;flex-direction:column;gap:10px;}
+.import-textarea{flex:1;min-height:140px;background:var(--card);
+  border:1.5px solid var(--border);border-radius:12px;padding:10px 12px;
+  color:var(--text);font-family:'Rubik',sans-serif;font-size:14px;
+  outline:none;resize:none;direction:rtl;}
+.import-textarea:focus{border-color:var(--accent)}
+/* ── IMPORT PREVIEW ── */
+.import-textarea-view,.import-preview-view{display:flex;flex-direction:column;gap:10px;flex:1}
+.import-preview-view{display:none}
+.import-preview-list{flex:1;overflow-y:auto;display:flex;flex-direction:column;gap:6px;min-height:80px;max-height:220px}
+.import-preview-row{display:flex;align-items:center;gap:8px;padding:7px 10px;
+  background:var(--card);border-radius:10px;font-size:14px}
+.import-preview-missing{opacity:.5}
+.import-preview-icon{flex-shrink:0;font-size:16px}
+.import-preview-name{flex:1;direction:rtl}
+.import-preview-note{font-size:11px;color:var(--muted);flex-shrink:0}
 /* ── BRAND PICKER TRIGGER ── */
 .brand-pick-btn{background:var(--card);border:1.5px solid var(--border);
   border-radius:11px;width:40px;height:40px;font-size:16px;cursor:pointer;
@@ -1897,6 +1924,7 @@ body.sheet-open{overflow:hidden!important;touch-action:none!important}
       maxlength="50" onkeydown="if(event.key==='Enter')addItem()"
       oninput="_onAddInputChange()">
     <button class="brand-pick-btn" id="brand-pick-btn" onclick="openBrandPicker()" title="בחר מותג ספציפי">🔍</button>
+    <button class="import-btn" id="import-list-btn" onclick="openImportModal()" title="ייבוא רשימה מוואטסאפ">📋</button>
     <button class="add-btn" data-testid="add-item-btn" onclick="addItem()">+</button>
   </div>
   <div id="list-panel" style="flex:1;overflow:hidden;display:flex;flex-direction:column">
@@ -4938,6 +4966,131 @@ window.toggleBought = async function(id) {
   }
 };
 
+// ── Text import (WhatsApp / SMS) ─────────────────────────────────────────
+// Parse order: blank → checkbox (existing) → NL-remove → NL-bought → NL-add → plain (existing)
+function _parseImportLines(raw) {
+  const CHECKBOX_RE  = /^[✅☑✔]\s*/u;
+  const NOISE_RE     = /^[-–—•*#\s]*$/u;
+  const BULLET_RE    = /^[*\-–—•]\s+/u;
+  const NL_REMOVE_RE = /^(מחק|תמחק|תוריד|להוריד|הסר|לא צריך|אין צורך ב|בלי)\s+/u;
+  const NL_BOUGHT_RE = /^(קניתי|כבר קניתי|סיימתי לקנות|כבר יש|נקנה|כבר קנינו)\s+/u;
+  const NL_ADD_RE    = /^(לקנות|צריך|תוסיף|להוסיף|קנה|קני|נצטרך|צריכים|תקני|תקנה)\s+/u;
+  const result = [];
+  for (const line of raw.split('\n')) {
+    const t = line.trim();
+    if (!t || NOISE_RE.test(t)) continue;
+    if (CHECKBOX_RE.test(t)) {
+      const n = t.replace(CHECKBOX_RE, '').trim();
+      if (n) result.push({ name: n, action: 'bought' });
+    } else if (NL_REMOVE_RE.test(t)) {
+      const n = t.replace(NL_REMOVE_RE, '').trim();
+      if (n) result.push({ name: n, action: 'remove' });
+    } else if (NL_BOUGHT_RE.test(t)) {
+      const n = t.replace(NL_BOUGHT_RE, '').trim();
+      if (n) result.push({ name: n, action: 'bought' });
+    } else if (NL_ADD_RE.test(t)) {
+      const n = t.replace(NL_ADD_RE, '').trim();
+      if (n) result.push({ name: n, action: 'add' });
+    } else {
+      const n = t.replace(BULLET_RE, '').trim();
+      if (n) result.push({ name: n, action: 'add' });
+    }
+  }
+  return result;
+}
+
+window.openImportModal = function() {
+  const el = document.getElementById('import-overlay');
+  if (el) el.style.display = 'flex';
+  _importShowTextarea();
+  const ta = document.getElementById('import-textarea');
+  if (ta) { ta.value = ''; setTimeout(() => ta.focus(), 50); }
+};
+
+window.closeImportModal = function() {
+  const el = document.getElementById('import-overlay');
+  if (el) el.style.display = 'none';
+  window._importParsed = null;
+};
+
+function _importShowTextarea() {
+  const tv = document.getElementById('import-textarea-view');
+  const pv = document.getElementById('import-preview-view');
+  if (tv) tv.style.display = 'flex';
+  if (pv) pv.style.display = 'none';
+}
+
+function _importShowPreview(parsed) {
+  const tv = document.getElementById('import-textarea-view');
+  const pv = document.getElementById('import-preview-view');
+  if (tv) tv.style.display = 'none';
+  if (!pv) return;
+  pv.style.display = 'flex';
+  const ICON = { add: '➕', bought: '✅', remove: '🗑' };
+  const liveNames = Object.values(items).map(i => (i.name || '').trim().toLowerCase());
+  const html = parsed.map(({ name, action }) => {
+    const missing = action === 'remove' && !liveNames.includes(name.toLowerCase());
+    return `<div class="import-preview-row${missing ? ' import-preview-missing' : ''}">` +
+      `<span class="import-preview-icon">${ICON[action]}</span>` +
+      `<span class="import-preview-name">${esc(name)}</span>` +
+      (missing ? `<span class="import-preview-note">לא ברשימה</span>` : '') +
+      `</div>`;
+  }).join('');
+  document.getElementById('import-preview-list').innerHTML =
+    html || `<div style="color:var(--muted);text-align:center;padding:16px">לא נמצאו פריטים</div>`;
+  window._importParsed = parsed;
+}
+
+// Called by "ייבא לרשימה" button — parses and shows preview
+window.runImport = function() {
+  const raw = document.getElementById('import-textarea')?.value || '';
+  const parsed = _parseImportLines(raw);
+  if (!parsed.length) { toast('⚠️ לא נמצאו פריטים לייבוא'); return; }
+  _importShowPreview(parsed);
+};
+
+window.backToImportEdit = function() { _importShowTextarea(); };
+
+// Called by "אשר ובצע" button — executes the previewed actions
+window.executeImport = function() {
+  const parsed = window._importParsed || [];
+  const m = myProfile || {};
+  let added = 0, bought = 0, removed = 0;
+  for (const { name, action } of parsed) {
+    if (action === 'remove') {
+      const id = Object.keys(items).find(k =>
+        (items[k].name || '').trim().toLowerCase() === name.toLowerCase());
+      if (id) {
+        remove(ref(db, `groups/${groupId}/items/${id}`));
+        logActivity('item_deleted', id, name);
+        removed++;
+      }
+    } else {
+      const isBought = action === 'bought';
+      const newRef = push(ref(db, `groups/${groupId}/items`));
+      set(newRef, {
+        name, qty: 1, bought: isBought, fav: false, barcode: null,
+        addedByUserId:      myId,
+        addedByDisplayName: myName,
+        addedByAvatarType:  m.avatarType  || 'emoji',
+        addedByAvatarValue: m.avatarValue || '👤',
+        addedByAvatarEmoji: m.avatarEmoji || null,
+        addedAt: Date.now(),
+        ts:      Date.now(),
+        ...(isBought ? { checkedByUserId: myId, checkedAt: Date.now() } : {}),
+      });
+      if (!isBought) logActivity('item_added', newRef.key, name);
+      isBought ? bought++ : added++;
+    }
+  }
+  closeImportModal();
+  const parts = [];
+  if (added)   parts.push(`➕ ${added}`);
+  if (bought)  parts.push(`✅ ${bought}`);
+  if (removed) parts.push(`🗑 ${removed}`);
+  toast(parts.length ? `📋 יובאו: ${parts.join(' · ')}` : '⚠️ לא בוצעו פעולות');
+};
+
 // Override deleteItem to log activity
 const _origDeleteItem = window.deleteItem;
 
@@ -9457,5 +9610,37 @@ window.applyTheme = function(theme) {
   </div>
 </div>
 
+<!-- ═══ TEXT IMPORT MODAL (WhatsApp / SMS) ═══ -->
+<div class="import-overlay" id="import-overlay" onclick="if(event.target===this)closeImportModal()">
+  <div class="import-modal">
+    <div class="modal-handle"></div>
+    <div class="modal-title">📋 ייבוא רשימה</div>
+
+    <!-- View 1: textarea input -->
+    <div class="import-textarea-view" id="import-textarea-view">
+      <div style="font-size:12px;color:var(--muted);margin-top:-4px">
+        הדבק רשימה מוואטסאפ או SMS — שורה אחת לכל פריט.<br>
+        שורות עם ✅ יסומנו כנקנו.
+      </div>
+      <textarea class="import-textarea" id="import-textarea"
+        placeholder="חלב&#10;לחם&#10;✅ ביצים&#10;מחק חמאה"></textarea>
+      <div class="modal-row">
+        <button class="mbtn primary" onclick="runImport()">📋 המשך</button>
+        <button class="mbtn ghost" onclick="closeImportModal()">ביטול</button>
+      </div>
+    </div>
+
+    <!-- View 2: preview before execute -->
+    <div class="import-preview-view" id="import-preview-view">
+      <div style="font-size:12px;color:var(--muted);margin-top:-4px">אשר את הפעולות הבאות:</div>
+      <div class="import-preview-list" id="import-preview-list"></div>
+      <div class="modal-row">
+        <button class="mbtn primary" onclick="executeImport()">✅ אשר ובצע</button>
+        <button class="mbtn ghost" onclick="backToImportEdit()">◀ חזור</button>
+      </div>
+    </div>
+  </div>
+</div>
+
 </body>
 </html>
