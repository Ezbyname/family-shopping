<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Transparent splash while TWA loads — ensures no white flash -->
    <style name="Theme.FamilyShopping" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/splashBg</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:statusBarColor">#0d1117</item>
        <item name="android:navigationBarColor">#0d1117</item>
    </style>
</resources>
