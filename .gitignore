node_modules/

# ── Secret files — NEVER commit these ─────────────────────────────────────────
.env
.env.local
.env.*.local
workers/prices/.env
workers/prices/.env.local
scripts/.env
scripts/.env.local

# ── Capacitor ─────────────────────────────────────────────────────────────────
# android-cap/ is the versioned Capacitor Android project (committed).
# Generated build outputs inside it are excluded:
android-cap/.gradle/
android-cap/app/build/
android-cap/build/

# ── Build artefacts ───────────────────────────────────────────────────────────
package-lock.json
*.log
.DS_Store
Thumbs.db
.vercel
__pycache__/
*.pyc
*.pyo

# ── Test output ───────────────────────────────────────────────────────────────
tests/playwright-report/
tests/test-results/

# ── Generated data files ──────────────────────────────────────────────────────
# Store snapshot — generated file, updated monthly on VPS
workers/prices/data/stores-geocoded.json
apply-import.py

# -- Capacitor www placeholder ------------------------------------------------
www/
